apiVersion: v1
kind: Service
metadata:
  name: {{ include "cotizacion-service.fullname" . }}
  labels:
    {{- include "cotizacion-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cotizacion-service.selectorLabels" . | nindent 4 }}
