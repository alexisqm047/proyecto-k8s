apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cotizacion-service.fullname" . }}-test-connection"
  labels:
    {{- include "cotizacion-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cotizacion-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
